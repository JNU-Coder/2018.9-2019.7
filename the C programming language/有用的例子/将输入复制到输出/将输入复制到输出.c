#include<stdio.h>
int main(void)
{
	int c,v;
	while ((c=getchar())!=EOF)
	    {
	    	if (c!=' ');
	    	    putchar(c);
	    	if (c==' ')
	    	   if (v!=' ')
	    	       putchar(c);
	    	v=c;
		}
 } 
