#include<stdio.h>
void select(int*,int);
int main(void)
{
	int s[10],*p;
	int i;
	p=s;
	printf("Enter the s:\n");
	for(i-=0;i<10;i++)
		scanf("%d",&s[i]);
	select(p,10);
	printf("the sortedms:\n");
	for(i=0;i<10;i++)
		printf("%d\n",s[i]);
	return 0;
}
void select(int x[],int n)
{
	int i,j,k;
	int t;
	for(i=0;i<n-1;i++)
	{
		k=i;
		for(j=i+1;j<n;j++)
			if(x[j]<x[k])	k=j;
			if(k!=i){
				t=x[i];
				x[i]=x[k];
				x[k]=t;
			}
	}
}

































































































































































































































































































































































































































































































































